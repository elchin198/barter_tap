
import { Link } from "wouter";

type LogoProps = {
  variant?: "dark" | "light";
};

export function Logo({ variant = "dark" }: LogoProps) {
  // Component removed as per client request to avoid duplicate logos
  return null;
}

export default Logo;
